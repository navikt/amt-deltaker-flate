// This configuration only applies to the package manager root.
import base from '@amt-deltaker-flate/eslint-config/eslint.config.js'

/** @type {import("eslint").Linter.Config[]} */
export default [...base]